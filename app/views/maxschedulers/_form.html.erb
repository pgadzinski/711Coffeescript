<%= form_for(@maxscheduler) do |f| %>
  <% if @maxscheduler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maxscheduler.errors.count, "error") %> prohibited this maxscheduler from being saved:</h2>

      <ul>
      <% @maxscheduler.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :paying %><br />
    <%= f.text_field :paying %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
