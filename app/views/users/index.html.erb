<h1>Listing of Users</h1>
<br>
<b>Screen for setting up different user accounts for access to MaxScheduler. </b>
<br>
<br>
<table class="table table-striped table-bordered table-condensed">
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
    <th>Color</th>
    <th>Schedule start date</th>
    <th>Num. of weeks</th>
    <th>Time zone</th>
    <th>Current Site</th>
    <th>Current Board</th>    
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.FirstName %></td>
    <td><%= user.LastName %></td>
    <td><%= user.email %></td>
    <td><%= user.color %></td>
    <td><%= user.schedStartDate %></td>
    <td><%= user.numberOfWeeks %></td>
    <td><%= user.timeZone %></td>
    <td><%= Site.find(user.currentSite).name %></td>
    <td><%= Board.find(user.currentBoard).name %></td>
    <td><%= link_to 'Edit', edit_user_path(user), :class => 'btn btn-info'  %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New User', new_user_path %>
<br />
