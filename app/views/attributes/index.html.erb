<h1>Listing of attributes</h1>
<br>
<b>For each job to schedule, there are often a few values that go with each job. Here you can define the labels for the different types of job data, examples of attributes are: JobNum, Addr, Color, CustNam, etc.</b>
<br>
<br>
<table class="table table-striped table-bordered table-condensed">
  <tr>
    <th>Name</th>
    <th>Importposition</th>
    <th>Listposition</th>
    <th>Scheduleposition</th>
    <th>ColumnWidth</th>
    <th></th>
    <th></th>
  </tr>

<% @attributes.each do |attribute| %>
  <tr>
    <td><%= attribute.name %></td>
    <td><%= attribute.importposition %></td>
    <td><%= attribute.listposition %></td>
    <td><%= attribute.scheduleposition %></td>
    <td><%= attribute.columnWidth %></td>
    <td><%= link_to 'Edit', edit_attribute_path(attribute), :class => 'btn btn-info'  %></td>
    <td><%= link_to 'Destroy', attribute, :class => 'btn btn-danger', confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>

<%= form_for(@attribute) do |f| %>

  <% if @attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute.errors.count, "error") %> prohibited this attribute from being saved:</h2>

      <ul>
      <% @attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <tr><td colspan=7><b>Add a new entry below:</b></td></tr>

  <tr> <%= f.hidden_field :maxscheduler_id, :value => @maxschedulerId %>
  <td><%= f.text_field :name%></td>
  <td><%= f.number_field :importposition, :style => 'width: 50px;'   %></td>
  <td><%= f.number_field :listposition, :style => 'width: 50px;'   %></td>
  <td><%= f.number_field :scheduleposition, :style => 'width: 50px;'   %></td>
  <td><%= f.number_field :columnWidth, :style => 'width: 50px;'   %></td>
  <td></td>
  <td></td>
  </tr>

  <tr><td colspan=7><%= f.submit nil , :class => 'btn btn-primary'  %></td></tr>


<% end %>

</table>


<br><br>
